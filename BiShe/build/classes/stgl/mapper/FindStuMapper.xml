<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="stgl.mapper.FindStuMapper">
  	<select id="findStu" resultType="Stgl_Stu">
  		select * from STGL_STU where stu_username=#{stu_username}
 	</select>
  </mapper>